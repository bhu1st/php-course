<?php    	//open parent directory	$handle = opendir('..');	//if success    if ($handle) {        		while (false !== ($file = readdir($handle))) {					//read directory with readdir, assign the read file to $file, then echo it.             echo "$file<br/>";			//loop until readdir finishes reading directory        }        		//close the open directory		closedir($handle);    }?>